# BMAD Project Configuration

project:
  name: "AMPH Academy"
  type: "web-app"  # web-app, mobile-app, api, game, library, cli, other
  level: 3    # 0-4
  description: "Amazon PPC learning hub for Filipino VAs"

paths:
  output_folder: "docs"
  stories_folder: "docs/stories"
  status_file: "bmad/workflow-status.yaml"
  sprint_file: "bmad/sprint-status.yaml"

language:
  communication_language: "English"
  document_output_language: "English"

workflow:
  phase_1_analysis:
    product_brief: "required"  # required, recommended, optional
    research: "optional"
    brainstorm: "optional"

  phase_2_planning:
    prd: "required"
    tech_spec: "optional"
    ux_design: "recommended"

  phase_3_solutioning:
    architecture: "required"
    gate_check: "recommended"

  phase_4_implementation:
    sprint_planning: "required"
    stories: "required"

metadata:
  created: "2026-07-01T20:21:36Z"
  bmad_version: "6.0.0"
  last_updated: "2026-07-02"
